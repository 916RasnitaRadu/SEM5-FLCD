# Program that calculates sum of the numbers smaller than an input number

integer a;
start 

read(a);

integer sum;
sum = 0;

for (integer i = 1; i < a; i = i + 1) {
    sum = sum + i;
}

print(sum);